# Audit & AI model module — 2026-04-06

## Ngày

2026-04-06

## Mục đích

Rà soát module quản lý mô hình AI, sửa lỗi ưu tiên (trùng `AIModelManager`, parser `ollama list`), tách logic test được, và ghi backlog ý tưởng phát triển.

## File thay đổi / thêm

| Path | Ghi chú |
|------|---------|
| [Sources/AIModelKit/AIModelTypes.swift](../Sources/AIModelKit/AIModelTypes.swift) | Kiểu dữ liệu public (Ollama / LM Studio) |
| [Sources/AIModelKit/AIModelParsing.swift](../Sources/AIModelKit/AIModelParsing.swift) | `parseByteCount`, `parseOllamaList` (regex hỗ trợ GiB/MiB) |
| [Sources/AIModelKitVerify/main.swift](../Sources/AIModelKitVerify/main.swift) | Smoke test khi không có XCTest (Command Line Tools) |
| [AppUninstaller/AIModelManager.swift](../AppUninstaller/AIModelManager.swift) | Import `AIModelKit`, bỏ trùng type |
| [AppUninstaller/AIModelsView.swift](../AppUninstaller/AIModelsView.swift) | `@EnvironmentObject` thay `@StateObject` |
| [AppUninstaller/SmartCleanerView.swift](../AppUninstaller/SmartCleanerView.swift) | `@EnvironmentObject` |
| [AppUninstaller/ContentView.swift](../AppUninstaller/ContentView.swift) | `@StateObject` + `.environmentObject(aiModelManager)` |
| [Package.swift](../Package.swift) | Target `AIModelKit`, `AIModelKitVerify`, test phụ thuộc `AIModelKit` |
| [build.sh](../build.sh) | Chuyển từ `swiftc` snapshot sang `swift build -c release` + chép `MacOptimizer_AppUninstaller.bundle` |
| [Tests/AppUninstallerTests/AIModelParsingTests.swift](../Tests/AppUninstallerTests/AIModelParsingTests.swift) | XCTest (chỉ build khi có `canImport(XCTest)`) |
| `AppUninstaller/AIModelParsing.swift` | Xóa (đã chuyển sang `AIModelKit`) |

## Bug đã xử lý (tóm tắt)

1. **P1 — Hai `AIModelManager`:** `SmartCleanerView` và `AIModelsView` mỗi nơi tạo `@StateObject` → quét lặp, tốn I/O. **Fix:** một instance ở `ContentView`, inject qua `environmentObject`.
2. **P1 — Parser `ollama list`:** Regex cũ không khớp `GiB`/`MiB`; `parseByteCount` chỉ decimal. **Fix:** regex mới + đơn vị nhị phân KiB…PiB.
3. **P2 — Tên model rỗng:** Bỏ qua dòng không parse được tên (guard `!name.isEmpty`).

## Cập nhật sau — loading khi chuyển module (2026-04-06)

- **File:** [`AppUninstaller/ContentView.swift`](../AppUninstaller/ContentView.swift)
- **Hành vi:** Khi click mục sidebar (hoặc mở module qua notification), vùng nội dung chính hiển thị overlay mờ + `ProgressView` trong ~0.38s (khớp animation chuyển cảnh), tránh cảm giác “đơ” khi view nặng mount.
- **Test:** `swift build`; chạy app, bấm qua lại các module và kiểm tra overlay hiện rồi tắt.

## Cách test lại

```bash
cd /path/to/MacOptimizervn
swift build
swift run AIModelKitVerify
```

- Với **Xcode.app** đầy đủ: `swift test` chạy XCTest trong `AppUninstallerTests`.
- Chỉ **Command Line Tools**: `swift test` build nhưng không có XCTest; dùng `AIModelKitVerify` làm kiểm chứng parsing.

## Backlog ý tưởng (ưu tiên gợi ý)

| Ưu tiên | Ý tưởng |
|--------|---------|
| Trung bình | Quét thêm cache Hugging Face (`~/.cache/huggingface`), MLX nếu có đường dẫn chuẩn |
| Trung bình | Làm mới `AIModelManager` khi `onAppear` màn Mô hình AI nếu cần dữ liệu mới nhất sau khi pull |
| Thấp | Hoàn TODO `RecoveryManager` (phục hồi thùng rác), `SmartScanLegacySupport` (bỏ qua) |
| Thấp | Rà soát tập trung `Process`/`shell` toàn app (đã liệt kê trong audit) |

## Rủi ro còn lại

- Định dạng `ollama list` đổi trong tương lai có thể cần cập nhật regex/parser.
- `swift test` không chạy được trên môi trường chỉ CLT — cần Xcode hoặc `AIModelKitVerify`.
