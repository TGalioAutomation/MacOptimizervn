# Brand assets

## App icon

- `AppIcon-master-1024.png` — master artwork (1024×1024) used to build `AppIcon.icns`.

Regenerate the `.icns` after editing the master:

```bash
./scripts/generate_app_icon.sh
```

The script uses `sips` and `iconutil` (macOS).
